# MCP Server SDK
mcp>=0.9.0

# Async I/O support (usually included with Python 3.7+)
# asyncio is built-in, no need to list

# Optional: for development and testing
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
